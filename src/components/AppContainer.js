import React from "react";
import AppPage from "./AppPage";
import Dock from "./Dock";

const AppContainer = () => {
  return (
    <div>
      <AppPage />
      <Dock />
    </div>
  );
};

export default AppContainer;
